import 'package:flutter/material.dart';

class NavigatorXScreen extends StatelessWidget {
  const NavigatorXScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("X Ekranı"),
      ),
    );
  }
}
